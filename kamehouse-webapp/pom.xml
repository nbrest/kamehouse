<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.nicobrest</groupId>
    <artifactId>kame-house-parent</artifactId>
    <version>1.07.1-SNAPSHOT</version>
  </parent>

  <name>KameHouse - Webapp</name>
  <groupId>com.nicobrest</groupId>
  <artifactId>kame-house-webapp</artifactId>
  <version>1.07.1-SNAPSHOT</version>
  
  <packaging>war</packaging>

  <build>
    <finalName>kame-house</finalName>
  </build>

  <!-- Profile definitions -->
  <profiles>
    <profile>
      <!-- Default profile -->
      <id>prod</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <filters>
          <filter>src/main/filters/prod/hibernate.properties</filter>
          <filter>src/main/filters/prod/jdbc.properties</filter>
        </filters>
      </build>
    </profile>

    <profile>
      <id>qa</id>
      <build>
        <filters>
          <filter>src/main/filters/qa/hibernate.properties</filter>
          <filter>src/main/filters/qa/jdbc.properties</filter>
        </filters>
      </build>
    </profile>

    <profile>
      <id>dev</id>
      <build>
        <filters>
          <filter>src/main/filters/dev/hibernate.properties</filter>
          <filter>src/main/filters/dev/jdbc.properties</filter>
        </filters>
      </build>
    </profile>
  </profiles>

  <dependencies>
    <dependency>
      <groupId>com.nicobrest</groupId>
      <artifactId>kame-house-commons</artifactId>
      <version>1.07.1-SNAPSHOT</version>
    </dependency>

    <dependency>
      <groupId>com.nicobrest</groupId>
      <artifactId>kame-house-commons-test</artifactId>
      <version>1.07.1-SNAPSHOT</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

</project>
